import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    formContainer: {
      flex:1,
    },
    centerContent:{
      justifyContent:'center',
      alignItems:'center'
    }
  });
