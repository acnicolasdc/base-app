import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    formContainer: {
      flex: 1,
      justifyContent: "space-between",
      alignItems: "center",
      paddingBottom: "10%",
    },
    formContainerInput: {
      width: "100%",
    },
  });
