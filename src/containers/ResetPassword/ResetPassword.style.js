import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    formContainer: {
      flex:1,
      justifyContent:'space-between',
      alignItems:'center',
    },
    formContainerInput: {
      width:'100%'
    },
  });
