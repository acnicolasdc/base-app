import { StyleSheet } from "react-native";

export default () =>
  StyleSheet.create({
    buttonAdd: { 
    marginTop: 10,
    width:150,
    alignSelf:'center',
    height:60,
    fontSize:50
    }
  });
