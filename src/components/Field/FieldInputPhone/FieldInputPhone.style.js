import { StyleSheet } from "react-native";

export default (colors) =>
  StyleSheet.create({
    fieldStructure: {
      backgroundColor: colors.background,
      paddingBottom: 0,
      marginBottom: 0,
      height: 55
    },
  });
